# Pinned to the versions actually installed in .venv (python 3.11.10, macOS
# arm64). Regenerate with: .venv/bin/python -m pip freeze
#
# torch has no CUDA build on Apple Silicon; on a CUDA machine install torch
# from the appropriate index instead of taking the default wheel:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu124

torch==2.12.0
numpy==2.4.6
PyYAML==6.0.3
pandas==3.0.3

# dev / CI
pytest==9.1.1
ruff==0.16.3
