# tiny-transformer

## overview
this project investigates how systems-level constraints (GPU memory bandwidth, compute utilization, and kernel efficiency) interact with optimization dynamics (batch size, learning rate, gradient noise, and convergence behavior) during transformer training.

we study how engineering choices simultaneously affect:
- training efficiency (throughput, hardware utilization)
- optimization behavior (validation loss, convergence speed)

